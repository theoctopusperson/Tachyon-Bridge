# Database - For local testing only
# If using Docker: DATABASE_URL=postgres://postgres:password@localhost:5432/tachyonbridge
# If using your Fly Postgres locally: get connection string from `fly postgres connect -a tachyonbridge-db`
DATABASE_URL=postgres://postgres:password@localhost:5432/tachyonbridge

# Groq API (required) - Get free API key from https://console.groq.com/
GROQ_API_KEY=gsk_your-api-key-here

# Server
PORT=3000

# ===================================================================
# Advanced: Manual testing only
# ===================================================================
# RACE_ID - Only needed if running `npm run cycle` directly
# Leave this commented out for normal web UI testing
# Example: RACE_ID=zephyrians npm run cycle
# ===================================================================
